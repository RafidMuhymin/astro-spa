---
import fs from "node:fs";
import { minify } from "terser";
import buildScript from "./script.js";

export interface props {
  beautify: boolean;
  limit: number;
  external: boolean;
  ignores: string[];
  highPriorityPrefetch: boolean;
  root: string;
  rootMargin: string;
  threshold: number;
  timeout: number;
  delay: number;
}

const {
  beautify = false,
  limit,
  external = false,
  ignores,
  highPriorityPrefetch,
  root,
  rootMargin,
  threshold = 0.25,
  timeout = 2000,
  delay = 500,
} = Astro.props;

const scriptContent = buildScript(
  limit,
  ignores,
  highPriorityPrefetch,
  root,
  rootMargin,
  threshold,
  timeout,
  delay
);

script = minify(scriptContent, { format : { beautify } });

fs.writeFileSync("node_modules/astro-spa/spa.js", script);

const scriptTag = `<script${
  external ? ` src="node_modules/astro-spa/spa.js"` : ""
}>${external ? "" : script}</script>`;
---

{scriptTag}